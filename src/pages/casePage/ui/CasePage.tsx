// import { ACCORDION_DATA } from '@/shared/ui/accordion/data/AccordionData';
import { Cases } from '@/widgets/cases';

const CasePage = () => {
  return (
    <section>
      <Cases />
    </section>
  );
};

export default CasePage;
